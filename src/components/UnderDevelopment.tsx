import { Box } from '@mui/material';

const UnderDevelopment = () => {
  return (
    <Box
      sx={{
        height: '100vh',
        display: 'grid',
        placeItems: 'center',
      }}
    >
      <p>Feltöltés alatt..</p>
    </Box>
  );
};

export default UnderDevelopment;
